# Experiments

Use this directory for reproducible experiment notes, lightweight configs, and
run documentation. Store bulky outputs under ignored paths such as `outputs/`,
`runs/`, `checkpoints/`, or `models/`.
